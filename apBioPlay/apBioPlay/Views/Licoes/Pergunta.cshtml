
@{
    Layout = null;

    int indR = 0;
}

<!DOCTYPE html>

<html>
<head>
    <link rel="stylesheet" href="~/Content/pergunta.css" />
    <meta name="viewport" content="width=device-width" />
    <title>Pergunta</title>
</head>
<body>
    <section id="home-hero">
        <div class="container">

            <h1>Selecione o lixo correto</h1>

            <form action="" method="post">
                <input type="text" hidden id="selecionada" value="@indR">
                <ul>
                    @for(int i=0; i<4; ++i)
                    {
                        var resp = ViewBag.respostas[ind]
                        string p = "pic" + (ind+1);
                        string r = "resp" + (ind + 1);
                        <li>
                            <input type="radio" name="pic" id="@p" />
                            <label for="@p"><img src=@resp.UrlImagem></label>
                            <h1 id="@r">@resp.Texto</h1>
                        </li>
                    }
                </ul>
            </form>

            <hr />

            @Html.ActionLink("Enviar Resposta", "ProximaPergunta", new { indResp = indR })

        </div>
    </section>
    <script src="~/Scripts/pergunta.js"></script>
</body>
</html>
